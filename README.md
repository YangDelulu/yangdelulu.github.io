# React + TypeScript + Vite

oxc (Rust) is used for on-save instead of Babel.

This is because i'm using Rolldown to bundle Vite, which is also Rust.

Faster and more consistent since they both use Rust.

## React Compiler

https://react.dev/learn/react-compiler/installation

The React Compiler is a heavier(?) compiler that performs a bunch of optimizations but obviously takes an overhead hit to achieve this. Not required for now but maybe once I add in my widget side projects.
